names=['max,maxwell,milan']
//ck whether max is present or not
console.log(names.inclu)
str='hello'
//num=19
//console.log(Array.from(str));
Array.from(str).map(char=>char.toUpperCase()).forEach(char=>console.log(char))
